// Where the API lives. Empty string means "same origin", which is correct when
// the server serves this page — locally, or on Vercel.
//
// The GitHub Pages workflow overwrites this file with the API's origin, because
// Pages can serve these files but cannot run the server.
window.KNOKNOK_API_BASE = "";
