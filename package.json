{
  "name": "knoknok",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tenant <-> landlord maintenance and to-do coordination, with an AI triage bot",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "seed": "bun run seed.ts",
    "test": "bun test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.120.0",
    "@libsql/client": "^0.17.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
